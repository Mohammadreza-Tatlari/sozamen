"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {money} from "@/lib/format";import {CartProduct} from "./AddToCart";
export function CheckoutForm({user}:{user:{name:string,phone:string,address:string}}){const router=useRouter();const[loading,setLoading]=useState(false);const[fail,setFail]=useState("");const cart:CartProduct[]=typeof window!=="undefined"?JSON.parse(localStorage.getItem("sozamen-cart")||"[]"):[];const total=cart.reduce((s,x)=>s+x.price*x.quantity,0);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const fd=new FormData(e.currentTarget);const r=await fetch("/api/orders",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:fd.get("name"),phone:fd.get("phone"),address:fd.get("address"),items:cart.map(x=>({productId:x.id,quantity:x.quantity}))})});const data=await r.json();if(r.ok){localStorage.removeItem("sozamen-cart");dispatchEvent(new Event("cart-change"));router.push(`/order-confirmation?id=${data.id}&ref=${data.reference}`)}else{setFail(data.error||"پرداخت انجام نشد");setLoading(false)}}return <form className="form-card" onSubmit={submit}><span className="section-kicker">پرداخت آزمایشی</span><h1>تکمیل سفارش</h1>{fail&&<div className="notice">{fail}</div>}<div className="field"><label>نام و نام خانوادگی</label><input name="name" defaultValue={user.name} required/></div><div className="field"><label>شماره موبایل</label><input name="phone" defaultValue={user.phone} required/></div><div className="field"><label>نشانی ارسال</label><textarea name="address" defaultValue={user.address} required/></div><div className="summary"><b>مبلغ پرداخت: {money(total)}</b><button className="button" disabled={loading||!cart.length}>{loading?"در حال پرداخت...":"پرداخت و ثبت سفارش"}</button></div></form>}
